datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Candidate {
  id     Int    @id @default(autoincrement())
  name   String
  email  String @unique
  mobno  String
  resume Bytes
}
